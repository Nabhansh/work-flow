name: Daily Auto Commit

on:
  schedule:
    - cron: "0 5,17 * * *"  # Runs at 5 AM and 5 PM UTC (≈ 10:30 AM & 10:30 PM IST)
  workflow_dispatch:  # allows you to run manually

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Make a dummy change
        run: |
          echo "$(date)" >> commitlog.txt

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add commitlog.txt
          git commit -m "auto commit at $(date)" || echo "No changes to commit"
          git push
